[bot]
name = "LuaBot"
prefix = "/"

[account]
# The bot will auto-generate a Unix socket in /tmp based on the phone number
phone = "+1234567890"

[plugins]
directory = "../../default-plugins"

[[commands]]
trigger = "ping"
response = "🏓 Pong!"
description = "Check if the bot is alive"
